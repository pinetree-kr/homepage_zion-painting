/**
 * Supabase 클라이언트 통합 export
 * 
 * 서버 사이드와 클라이언트 사이드를 구분하여 사용하세요:
 * - 클라이언트 사이드: `import { supabase } from '@/src/shared/lib/supabase/client'`
 * - 서버 사이드: `import { createServerClient } from '@/src/shared/lib/supabase/server'`
 */

// 클라이언트 사이드 export
// export {
//   supabase,
//   checkSupabaseSession,
//   getSupabaseUser,
//   onAuthStateChange,
// } from './client';

// // // 서버 사이드 export
// // export { createServerClient } from './server';

// // 공통 설정 export
// export { supabaseUrl, supabasePublishableKey } from './config';

// // 타입 export
// export type { Database } from '../supabase-types';

